services:
  - type: web
    name: studysphere
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn studysphere.asgi:application -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: studysphere-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: redissConnectionString
      - key: PYTHON_VERSION
        value: "3.13"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DEBUG
        value: "False"
      - key: PYTHONPATH
        value: "/opt/render/project/src"
  - type: redis
    name: redis
    plan: free
    ipAllowList: []

databases:
  - name: studysphere-db
    databaseName: studysphere
    plan: free
    user: studysphere